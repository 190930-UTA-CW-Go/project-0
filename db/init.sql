create table customer (
    email varchar primary key,
    pass varchar, 
    first_name varchar,
    last_name varchar
);

create table employee (
    email varchar primary key,
    pass varchar, 
    first_name varchar,
    last_name varchar
);
